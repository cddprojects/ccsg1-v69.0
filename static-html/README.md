# Matching Remote — Static HTML version

Self-contained HTML export of the Matching Remote landing site. Open `index.html` in a browser, or serve this folder with any static file server.

Regenerate from the Next.js source:

```bash
npm run build:static-html
```
